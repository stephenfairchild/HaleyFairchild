name: 15-minute-cron
on:
  schedule:
    - cron: '*/15 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Sent updated list of newsletter subscribers
        run: |
          curl --request POST \
          --url 'https://haleyfairchild.com/api/subscribers' \
          --header 'Authorization: Bearer ${{ secrets.API_SECRET_KEY }}'
